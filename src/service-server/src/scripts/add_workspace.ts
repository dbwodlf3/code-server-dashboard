// get userId, containerName, password

const userId = 1;
const containerName = '';
const password = '';

let command = `docker service create --name ${containerName}`
// add service

// get expose url -> update expose url. and access.